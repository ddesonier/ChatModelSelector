# Azure OpenAI Configuration
# Get these values from your Azure OpenAI resource in the Azure portal
AZURE_OPENAI_ENDPOINT=https://your-aoai-resource.openai.azure.com/
AZURE_OPENAI_KEY=your-api-key-here
AZURE_OPENAI_API_VERSION=2024-06-01

# Azure Resource Management Configuration
# Use the discover-azure-resources.ps1 script to find these values
SUBSCRIPTION_ID=your-subscription-id-here
RESOURCE_GROUP_NAME=your-resource-group-name
AOAI_ACCOUNT_NAME=your-aoai-account-name

# Azure AD Authentication (Required for Docker containers)
# Create a Service Principal: az ad sp create-for-rbac --name "your-app-name" --role contributor
AZURE_CLIENT_ID=your-service-principal-client-id
AZURE_CLIENT_SECRET=your-service-principal-client-secret
AZURE_TENANT_ID=your-azure-tenant-id

# How to get these values:
# 1. Run: .\discover-azure-resources.ps1
# 2. Or check Azure portal > Your OpenAI resource > Overview tab
# 3. For API key: Azure portal > Your OpenAI resource > Keys and Endpoint
# 4. For Service Principal: Run the Azure CLI command above or use Azure portal
